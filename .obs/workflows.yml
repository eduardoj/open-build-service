workflow:
  steps:
    - branch_package:
        source_project: home:Admin
        source_package: ctris
  filters:
    architectures:
      only:
        - x86_64
    repositories:
      only:
        - openSUSE_Tumbleweed
        - SLE_15_SP3
